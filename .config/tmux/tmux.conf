# set -g default-terminal "xterm-256color"
# set -ag terminal-overrides ",xterm-256color:RGB"

# styles


# add spacing between and session name to status bar
# set -g status-left-length 64 

# set the left status bar
# set-option -g status-left '  #{window_list}'

# set the right status bar with the time and date in human readabl3
# set -g status-right '#{session_name} #{tmux_mode_indicator}   ' # #h is the hostname, #H is the full hostname, #S is the session name

# Set new panes to open in current directory
bind c new-window -c "#{pane_current_path}"
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

#set -g default-command /usr/bin/fish
#set -g default-shell /usr/bin/fish 

# Mouse scroll support on Tmux (disable it on your terminal emulator if needed)
set -g mouse on

# Setting base window number to 1 
set -g base-index 1
set -g pane-base-index 1
set-option -g renumber-windows on

# NVIM integration config 
set-option -g focus-events on
# zero-out escape time delay
set-option -sg escape-time 0

# Sets default terminal for fixing colors inside Neovim 
set -g default-terminal "tmux-256color"
set -ga terminal-features ',xterm-kitty:RGB'
set -g terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -g terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  

set-option -g allow-rename off
set -g monitor-activity on

#---------------------------------------
# Theme
#---------------------------------------
# set -g status-style 'bg=#1F1F25 fg=#667788'
# set-window-option -g window-status-current-style bg=#363E4A
# set -g window-status-current-style fg=#44ccaa   
# set-option -g status-position "bottom"
# set-option -g status-justify centre
# set-option -g status-left '#[bg=default,fg=default,bold]#{?client_prefix,,  tmux  }#[bg=#1199aa,fg=black,bold]#{?client_prefix,  tmux  ,}'
# set-option -g status-right '#S'
# set-option -g window-status-format ' #I:#W '
# set-option -g window-status-current-format '#[bg=#{@minimal-tmux-bg},fg=#000000] #I:#W#{?window_zoomed_flag, 󰊓 , }'


# ---------------------------------------
# Plugins
# ---------------------------------------
set -g @plugin 'tmux-plugins/tpm' # Plugin manager 
# set -g @plugin 'rose-pine/tmux' # Rose Pine theme
set -g @plugin 'erikw/tmux-powerline'
set -g @plugin 'tmux-plugins/tmux-sensible' # Sensible default | Config de sentido común
set -g @plugin 'tmux-plugins/tmux-yank' # System clipboard for tmux yanks
set -g @plugin 'tmux-plugins/tmux-copycat' # System clipboard for tmux yanks
# set -g @plugin 'MunifTanjim/tmux-mode-indicator'

set -g @plugin 'nathanroark/nathan-tmux-theme'
# Dracula theme config
set -g @dracula-plugins "weather spotify-tui"
set -g @dracula-weather-colors "dark_purple white"
set -g @dracula-show-powerline true
set -g @dracula-refresh-rate 5
# set -g @dracula-border-contrast true
set -g @dracula-show-empty-plugins true
set -g @dracula-show-left-icon \uebc8

# Rosé Pine theme config
# set -g @rose_pine_variant 'main'
# set -g @rose_pine_directory 'on'
# set -g @rose_pine_show_current_program 'on'
# set -g @rose_pine_bar_bg_disable 'on'
# set -g @rose_pine_status_left_prepend_section '#{tmux_mode_indicator}'
# set -g @rose_pine_bar_bg_transparent_option '7'
# set -g @rose_pine_date_time '%H:%M | %a'
# set -g @rose_pine_disable_active_window_menu 'on'
# set -g @rose_pine_default_window_behavior 'on'
# set -g @rose_pine_show_pane_directory 'on'
# set -g @rose_pine_prioritize_windows 'on'
# set -g @rose_pine_width_to_hide '80'
# set -g @rose_pine_window_count '5'
# set -g @rose_pine_user 'on'
# set -g @rose_pine_host 'on'
# set -g @rose_pine_left_separator ' > '
# set -g @rose_pine_right_separator '  '
# set -g @rose_pine_active_window_color 'love'
# set -g @rose_pine_window_status_separator ''
# set -g @rose_pine_window_tabs_enabled 'on' -- OLD

# Set permanent sessions = on
# set -g @continuum-restore 'on'

# Initialise TMUX plugin manager
run '~/.tmux/plugins/tpm/tpm'
